*shiny의 구성 1. UI(user interface) 2. Server 3.Shiny App*
=> User-Server Model (= Client-Server Model)

# server가 하는 일: UI로부터 input을 받은 후, output을 UI로 보냄

## 구현 ...
library(shiny)
ui<-
server<-
shinyApp(ui = ui, server = server)




